<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gammaos.shadercontrol">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShaderControl">

        <!-- No launcher intent filter, so it will not appear in the app drawer -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
        </activity>

        <!-- GammaOS Nano "Freeze App Under Clock": a transparent activity nano fronts while the PSP
             slide clock is open so the running game receives onPause and pauses itself cleanly, then
             finishes (game onResume) when the clock closes. Translucent so the game stays in onPause
             (keeps its surface for a clean resume) rather than onStop. Hidden from recents, own task. -->
        <activity
            android:name=".NanoPauseActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:taskAffinity="com.gammaos.nano.pause"
            android:configChanges="orientation|screenSize|smallestScreenSize|keyboardHidden|screenLayout|uiMode|density|navigation" />

    </application>
</manifest>
