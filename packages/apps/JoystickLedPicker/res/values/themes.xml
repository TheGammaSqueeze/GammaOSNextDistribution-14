<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.JoystickLedPicker" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorAccent">@color/colorAccent</item>
        <!-- Ensure the activity/window picks up background/text even if a view doesn't set them -->
        <item name="android:windowBackground">@color/background</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
    </style>
</resources>
